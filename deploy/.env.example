# 复制为 deploy/.env（与 docker-compose.backend.yml 同目录，Compose 会自动加载）
# REPO_ROOT 必须指向 WHUSingularity 仓库根（含各模块 target/*.jar）
# Windows 正斜杠示例（按本机修改）:
# REPO_ROOT=D:/chuangxinshijian/WHUSingularity
#
# 打包 order（须带依赖 core）:
#   mvn -pl singularity-order -am package -DskipTests
