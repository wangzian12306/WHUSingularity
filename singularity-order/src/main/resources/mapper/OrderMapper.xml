<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.lubover.singularity.order.mapper.OrderMapper">

    <resultMap id="BaseResultMap" type="com.lubover.singularity.order.entity.Order">
        <id column="order_id" property="orderId" jdbcType="VARCHAR"/>
        <result column="user_id" property="userId" jdbcType="VARCHAR"/>
        <result column="slot_id" property="slotId" jdbcType="VARCHAR"/>
        <result column="product_id" property="productId" jdbcType="VARCHAR"/>
        <result column="status" property="status" jdbcType="VARCHAR"/>
        <result column="create_time" property="createTime" jdbcType="TIMESTAMP"/>
        <result column="update_time" property="updateTime" jdbcType="TIMESTAMP"/>
    </resultMap>

    <insert id="insert" parameterType="com.lubover.singularity.order.entity.Order">
        INSERT INTO `order` (order_id, user_id, slot_id, product_id, status, create_time, update_time)
        VALUES (#{orderId}, #{userId}, #{slotId}, #{productId}, #{status}, #{createTime}, #{updateTime})
    </insert>

    <select id="selectByOrderId" resultMap="BaseResultMap">
        SELECT order_id, user_id, slot_id, product_id, status, create_time, update_time
        FROM `order`
        WHERE order_id = #{orderId}
    </select>

    <update id="updateStatus">
        UPDATE `order`
        SET status = #{status}, update_time = NOW()
        WHERE order_id = #{orderId}
    </update>

    <select id="selectList" resultMap="BaseResultMap">
        SELECT order_id, user_id, slot_id, product_id, status, create_time, update_time
        FROM `order`
        <where>
            <if test="userId != null and userId != ''">
                AND user_id = #{userId}
            </if>
            <if test="status != null and status != ''">
                AND status = #{status}
            </if>
        </where>
        ORDER BY create_time DESC
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <select id="countList" resultType="long">
        SELECT COUNT(*)
        FROM `order`
        <where>
            <if test="userId != null and userId != ''">
                AND user_id = #{userId}
            </if>
            <if test="status != null and status != ''">
                AND status = #{status}
            </if>
        </where>
    </select>

</mapper>
