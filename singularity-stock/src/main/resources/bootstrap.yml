spring:
  application:
    name: singularity-stock
  cloud:
    nacos:
      server-addr: localhost:8848
      discovery:
        enabled: true
        namespace: public
        group: DEFAULT_GROUP
      config:
        enabled: true
        namespace: public
        group: DEFAULT_GROUP
        file-extension: yaml
        refresh-enabled: true
