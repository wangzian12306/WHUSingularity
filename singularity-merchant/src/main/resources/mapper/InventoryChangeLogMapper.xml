<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lubover.singularity.merchant.mapper.InventoryChangeLogMapper">

    <resultMap id="BaseResultMap" type="com.lubover.singularity.merchant.entity.InventoryChangeLog">
        <id column="id" property="id" />
        <result column="product_id" property="productId" />
        <result column="merchant_id" property="merchantId" />
        <result column="change_type" property="changeType" />
        <result column="change_quantity" property="changeQuantity" />
        <result column="before_quantity" property="beforeQuantity" />
        <result column="after_quantity" property="afterQuantity" />
        <result column="order_id" property="orderId" />
        <result column="remark" property="remark" />
        <result column="operator_id" property="operatorId" />
        <result column="operator_name" property="operatorName" />
        <result column="create_time" property="createTime" />
    </resultMap>

    <insert id="insert" parameterType="com.lubover.singularity.merchant.entity.InventoryChangeLog" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO inventory_change_log (product_id, merchant_id, change_type, change_quantity, before_quantity, after_quantity, order_id, remark, operator_id, operator_name, create_time)
        VALUES (#{productId}, #{merchantId}, #{changeType}, #{changeQuantity}, #{beforeQuantity}, #{afterQuantity}, #{orderId}, #{remark}, #{operatorId}, #{operatorName}, NOW())
    </insert>

    <select id="selectByProductId" resultMap="BaseResultMap">
        SELECT * FROM inventory_change_log WHERE product_id = #{productId} ORDER BY create_time DESC
    </select>

    <select id="selectByMerchantId" resultMap="BaseResultMap">
        SELECT * FROM inventory_change_log WHERE merchant_id = #{merchantId} ORDER BY create_time DESC
    </select>

</mapper>
