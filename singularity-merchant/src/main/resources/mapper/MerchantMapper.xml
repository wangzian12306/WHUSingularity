<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lubover.singularity.merchant.mapper.MerchantMapper">

    <resultMap id="BaseResultMap" type="com.lubover.singularity.merchant.entity.Merchant">
        <id column="id" property="id" />
        <result column="username" property="username" />
        <result column="password" property="password" />
        <result column="shop_name" property="shopName" />
        <result column="contact_name" property="contactName" />
        <result column="contact_phone" property="contactPhone" />
        <result column="address" property="address" />
        <result column="description" property="description" />
        <result column="status" property="status" />
        <result column="avatar" property="avatar" />
        <result column="create_time" property="createTime" />
        <result column="update_time" property="updateTime" />
    </resultMap>

    <insert id="insert" parameterType="com.lubover.singularity.merchant.entity.Merchant" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO merchant (username, password, shop_name, contact_name, contact_phone, address, description, status, avatar, create_time, update_time)
        VALUES (#{username}, #{password}, #{shopName}, #{contactName}, #{contactPhone}, #{address}, #{description}, #{status}, #{avatar}, NOW(), NOW())
    </insert>

    <update id="update" parameterType="com.lubover.singularity.merchant.entity.Merchant">
        UPDATE merchant
        <set>
            <if test="shopName != null">shop_name = #{shopName},</if>
            <if test="contactName != null">contact_name = #{contactName},</if>
            <if test="contactPhone != null">contact_phone = #{contactPhone},</if>
            <if test="address != null">address = #{address},</if>
            <if test="description != null">description = #{description},</if>
            <if test="avatar != null">avatar = #{avatar},</if>
            update_time = NOW()
        </set>
        WHERE id = #{id}
    </update>

    <update id="updateStatus">
        UPDATE merchant SET status = #{status}, update_time = NOW() WHERE id = #{id}
    </update>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT * FROM merchant WHERE id = #{id}
    </select>

    <select id="selectByUsername" resultMap="BaseResultMap">
        SELECT * FROM merchant WHERE username = #{username}
    </select>

</mapper>
